{
  "name": "nodejest",
  "version": "1.0.0",
  "description": "",
  "main": "./app.js",
  "scripts": {
    "dev": "nodemon src/server.js --ignore __tests__",
    "start": "node src/server.js",
    "pretest": "set NODE_ENV=test&&sequelize db:create&&sequelize db:migrate",
    "test": "set NODE_ENV=test&&jest",
    "postTest": "set NODE_ENV=test&&sequelize db:migrate:undo:all"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.12.11",
    "mysql2": "^2.2.5",
    "sequelize": "^6.6.2"
  },
  "devDependencies": {
    "jest": "^27.0.1",
    "sequelize-cli": "^6.2.0",
    "sqlite3": "^5.0.2"
  }
}
